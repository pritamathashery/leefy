<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/leafy_bg"  <!-- Background color -->
    android:padding="16dp">

    <!-- App Title or Welcome Text -->
    <TextView
        android:id="@+id/welcomeText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to Leafy!"
        android:textColor="@color/leafy_text"
        android:textSize="24sp"
        android:fontFamily="sans-serif-medium"
        android:layout_marginTop="100dp"/>

    <!-- Description or Call-to-Action Text -->
    <TextView
        android:id="@+id/descriptionText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Find the best plants for your climate."
        android:textColor="@color/leafy_slate"
        android:textSize="16sp"
        android:layout_marginTop="16dp"/>

    <!-- Button to Get Started -->
    <Button
        android:id="@+id/startButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Get Started"
        android:backgroundTint="@color/leafy_green"
        android:textColor="@color/leafy_text"
        android:layout_marginTop="32dp"/>
</LinearLayout>
